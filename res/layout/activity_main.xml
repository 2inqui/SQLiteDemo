<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/activity_main_button_add_comment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="98dp"
        android:onClick="addComment"
        android:text="@string/add_comment" />

    <Button
        android:id="@+id/activity_main_button_comments"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/activity_main_button_add_comment"
        android:layout_alignRight="@+id/activity_main_button_add_comment"
        android:layout_below="@+id/activity_main_button_add_comment"
        android:layout_marginTop="76dp"
        android:onClick="viewComments"
        android:text="@string/comments" />

</RelativeLayout>
